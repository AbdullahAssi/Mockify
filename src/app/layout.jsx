import './globals.css'
import Header from '@/components/Header'
import localFont from 'next/font/local'

export const metadata = {
  title: 'Mockify',
  description: 'Generated by create next app',
}

const neue = localFont({ 
  src: './localfont/HelveticaNeueLTPro-Roman.7216551.woff2',
  formats: ['woff2'],
  // fontWeight: 'normal',
  variable: "--font-neue",
  fontDisplay: 'swap', 
})
const Ppe = localFont({ 
  src: './localfont/PPEditorialNew-Regular.2512cb3.woff2',
  formats: ['woff2'],
  // fontWeight: 'normal',
  variable: "--font-ppe",
  fontDisplay: 'swap', 
})


export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body >
        <Header />
        <main className={`${Ppe.variable} ${neue.variable}`}>
        {children}
        </main>
        </body>
    </html>
  )
}
